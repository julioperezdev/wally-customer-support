# Non-secret local configuration for the API-owned Cognito login.
# The browser never receives a Cognito client secret or token.
VITE_WCS_BACKEND_BASE_URL=http://localhost:8080
VITE_WCS_AUTH_BASE_URL=/internal/auth
