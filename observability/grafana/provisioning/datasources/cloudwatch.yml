apiVersion: 1

prune: true

datasources:
  - name: WCS CloudWatch
    uid: wcs-cloudwatch
    type: cloudwatch
    access: proxy
    isDefault: true
    editable: false
    jsonData:
      authType: default
      defaultRegion: ${AWS_REGION}
      profile: ${AWS_PROFILE}
