wcs.whatsapp.adapter=mock
wcs.ai.provider=mock
wcs.rag.provider=mock

# Start PostgreSQL with docker compose for the local durable store.
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/wcs}
spring.datasource.username=${DATABASE_USERNAME:wcs}
spring.datasource.password=${DATABASE_PASSWORD:}
