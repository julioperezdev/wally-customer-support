package com.wally.customersupport.agent.application.activation;

public enum AgentActivationMutationStatus {
    ACTIVATED,
    KILL_SWITCHED,
    ROLLED_BACK,
    DISABLED,
    DENIED,
    INVALID,
    ALREADY_PROCESSED,
    FAILED
}
